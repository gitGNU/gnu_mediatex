@cindex configuration file

The @process{Conf} process handle queries about collections.
@itemize @bullet
@item 
A single query is used to create a new collection or to join
an existing remote collection. 
Be careful because a single query is used too, 
to remove a collection or to retract from a remote collection.
@item
Servers may be linked together over private networks. 
In this case, queries for private server will be relayed by the 
@acronym{NAT} gateway.
@item 
Supports may be share or withdraw to collections.
@end itemize

Process conceptual model:

@image{mediatex-figures/conf,,,,}

@itemize @bullet
@item Events in
@table @code
@item @eventClientConfAddColl
@itemx @eventClientConfDelColl
from 
@actorAdmin
@item @eventClientConfList
@itemx @eventClientConfWithdrawSupp
from 
@actorPublisher
@eventClientConfWithdrawSuppCall
from @procClientSupp
@end table

@item Events out
@table @code
@item @eventClientServUpgradeCall
to @procClientServ
@item @eventScriptsNew 
@itemx @eventScriptsFree
to @procScriptsNewFreeClean
@end table

@item Data in/out
@itemize @bullet
@item @dataConf
@end itemize
@end itemize

Examples:
see Scenarios (@ref{Scenario 2})

Code:
@table @file
@item src/client/conf.c
@end table
